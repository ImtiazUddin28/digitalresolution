/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['89.116.212.106', 'images.unsplash.com', 'api.softexsolution.com']
  }
}

module.exports = nextConfig
